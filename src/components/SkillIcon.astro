---
interface Props {
  icon: string;
}

const { icon } = Astro.props;
const iconUrl = `url('${import.meta.env.BASE_URL}/icons/tech/${icon}.svg')`;
---

<span class="skill-icon" style={`--icon-url: ${iconUrl};`} aria-hidden="true"></span>
