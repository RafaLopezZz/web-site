# M1 — RLP Design Foundation

## Objective

Establish the RLP design foundation before production visual implementation. This is an execution record, not a second design system; `DESIGN.md` remains the normative source.

## M1.1 — Design Doctrine and DESIGN.md

Create the normative design doctrine in `DESIGN.md` and record the M1 execution path. M1.1 is documentation-only and makes no production visual changes.

## M1.2 — Design Decision Promotion

Status: COMPLETE

Exploration decisions promoted to the normative doctrine:

- IBM Plex Sans is the editorial and UI family; IBM Plex Mono is technical system and evidence context only.
- `SiteHeader` is the sole navigation contract with RLP identity, semantic current state, bilingual access, and responsive content-fit behavior.
- Home is intentionally short and routes own their complete territory content.
- Primary, Secondary, and Text/Action Link establish the canonical action hierarchy.
- Surface grammar is semantic: common Surface, Artifact, Dossier, Record, and Explorer; universal generic Cards are prohibited.
- Hero CMD is bounded RLP identity chrome, not fabricated runtime activity.
- Responsive Home preserves semantic content from 320px through >=1024px while decorative chrome may adapt.
- Territory grammars define Home, Work, Production, Experience, Education, Notes, About, and Contact; evidence is real or omitted.
- Token intent families establish names only; M1.3 resolves implementation metrics and values.
- `Engineered, not generated.` remains an internal principle, never a footer slogan or public headline.

M1.2 is decision-evidence only. It establishes no production tokens, source changes, or implementation routing.

## M1.3 — Global UI Foundation

Implement the approved global UI foundation while preserving M0 behavior and validating the full regression suite and build.

### M1.3.1 — Global Design Tokens + Typography

Status: COMPLETE

- Canonical CSS properties: `--color-paper: #F2F0EA`, `--color-ink: #111315`, `--color-terminal: #0C0C0C`, `--color-rlp-cyan: #059AAF`, and `--color-graphite: #606568`.
- Tailwind semantic tokens: `paper`, `ink`, `terminal`, `rlp`, and `graphite`; `accent` remains a migration alias for canonical RLP Cyan.
- IBM Plex Sans is the global body and display family; IBM Plex Mono is the technical/code family. The `body` and `display` Tailwind aliases both resolve to Sans.
- Google Fonts loads Sans weights 400, 500, 600, and 700 plus Mono weights 400 and 500.
- Legacy dark canvas is intentionally preserved; this slice does not migrate the site globally to Paper.
- Focus, selection, and legacy global cyan uses now resolve to canonical RLP Cyan. No `#00E5FF`, DM Sans, or Syne remains in the authorized production design files.
- Focused foundation E2E: PASS (1 test); full E2E: PASS (13 tests); production build: PASS; `git diff --check`: PASS.

Next: M1.3.3 — Action / ActionLink

### M1.3.2 — SiteHeader

Status: COMPLETE

- `SiteHeader` is the single navigation implementation across Home and the established Blog routes; the legacy `Navbar` was removed after confirming those were its only consumers.
- The visible RLP brand links home. Valid transitional destinations are Home, Work (`#proyectos`), Experience (`#experiencia`), Education (`#formacion`), Notes (`/blog`), Contact (`#contacto`), and CV (`#cv`); no future routes or legacy Stack link remain.
- Home and Blog territory current state uses semantic `aria-current="page"`, restrained RLP Cyan, and a non-color bottom rule. B-002 is closed.
- The 390px menu is a keyboard-operable control with `aria-expanded`, `aria-controls`, changing accessible labels, link-close behavior, and Escape close behavior. Notes remains compatible with `/blog` and article routes.
- Focused SiteHeader E2E: PASS (3 tests); full E2E: PASS (16 tests); production build: PASS; `git diff --check`: PASS.
- Browser inspection: Home and Blog at 1024px and 390px had no horizontal overflow, visible/legible header and current state, and reachable mobile navigation.

### M1.3.3 — Action / ActionLink

Status: COMPLETE

- `Action` provides native Primary and Secondary submit/action buttons; `ActionLink` provides native Primary, Secondary, and Text navigation links while retaining valid anchor attributes such as `download`, `target`, and `rel`.
- The shared contract uses IBM Plex Sans, sharp geometry, Ink/Paper primary-secondary hierarchy, unfilled contextual links, restrained hover, and RLP Cyan plus non-color visible focus. It remains readable on the preserved legacy dark canvas.
- Authorized Home and Contact CTAs now use the primitives: Hero/CV, project and transition links, quick links, contact destinations, and the form submit action. SiteHeader, SiteFooter, and BlogPostCard remain untouched.
- Focused Action system E2E: PASS (4 tests); full E2E: PASS (20 tests); production build: PASS; `git diff --check`: PASS.
- Browser viewport inspection at 390px and 1024px: no horizontal overflow; framed actions retain practical targets, text links are non-pill, and contact destinations plus submit remain reachable.

Next: M1.3.4 — Surface Foundation

## M1.4 — Primitive Contracts and Gate

Implement and verify the named primitive contracts from `DESIGN.md`. M1.4 is the gate: accessible semantics, responsive preservation, no doctrine violations, regression evidence, production build evidence, and `git diff --check`.
