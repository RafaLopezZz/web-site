---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span
  class="inline-flex items-center border border-white/15 px-3 py-1 text-xs uppercase tracking-[0.18em] text-accent"
>
  {label}
</span>
