# M2 — Home

## Status

M2.1, M2.2, M2.3, M2.4, M2.5, and M2.6 are COMPLETE. The historical M2 Gate remains COMPLETE. M2.6 closed the corrective slice for the later post-M2 Explore + About expansion after maintainer visual acceptance.

Historical scope and evidence bullets below describe the state at each completed checkpoint. They do not override the current blocker ledger or later source-backed resolutions; in particular, B-003 is now closed by the ImportadorDB detailed case.

## M2.1 — Short Home Shell + Hero + CMD Identity

Home now contains the settled header, a short full-width Paper/Ink hero within the constrained `rlp-container`, route-local bounded CMD identity, and the settled footer. It does not render the legacy photo, project listings, Skills, About, Experience, Education, or Contact form. `CmdIdentity.astro` does not exist; the CMD markup is intentionally route-local.

### Evidence

- Focused RED: `Ver trabajo` resolved only to synthetic `#trabajo`, which had no meaningful destination.
- Focused: Home, action, and layout E2E cover the truthful existing `/blog/` destination, CV download anchor, full-width Paper canvas, constrained content, and responsive CMD composition.
- Full E2E: 20 accepted E2E checks.
- External maintainer visual evidence accepted: Home at 320px, 768px, and 1440px confirms Paper canvas, constrained content, and route-local CMD visuals.

### Superseded legacy Home assertions

- Project headings, artifacts, and related links.
- Experience and Education records.
- Contact availability, validation, captcha, success, and provider-failure behavior.
- The legacy Hero's `Ver proyectos` action and contact submit action.

These intentional Home characterization replacements remove only legacy projects, professional/Education records, Contact behavior, and the old Hero/contact CTAs from Home. Blog index/article preservation, SiteHeader, layout/design/action semantics, valid routes, and shared foundation behavior remain covered. No components or data were deleted.

### Scope boundary

Home is route-local Paper/Ink; other routes remain transitional. No new routes, featured evidence, explore region, or M2.2 work is included.

## M2.2 — Light/Dark/System Theme System

Status: COMPLETE. Manual visual review accepted.

### Implemented contract

- Locale is URL-driven only: `/` remains Spanish and `/en/` is the single English Home counterpart. `html[lang]` is exact per route; no locale storage, redirect, geolocation, flags, framework, or i18n dependency exists.
- `SiteHeader` supplies a compact 48×34px retro locale plate: real `ES`-left and `EN`-right anchors retain URL-driven `aria-current="page"`; an inset cyan route marker provides the secondary non-color current cue. Desktop explicitly sequences RLP → primary navigation → utilities with a normal flex gap; the centered utility row is locale → theme (and locale → theme → Menu on mobile), with no flags or stored locale state.
- English Home reuses the semantic Home/CMD presentation and translates only its bounded identity copy and actions. Its Header exposes only real progressive counterparts: English Home and the existing Spanish Blog; no future English routes or placeholders are emitted.
- Preference and effective theme are separate: `system`, `light`, or `dark` is stored as `rlp-theme`; absent or invalid storage safely resolves to `system`.
- `html` is the single authority with `data-theme-preference` and effective `data-theme`; synchronous defensive head bootstrap applies stored Dark before the first render and sets `color-scheme`.
- System follows `matchMedia("(prefers-color-scheme: dark)")` live; explicit Light and Dark ignore system changes.
- Semantic theme roles are `--theme-canvas`, `--theme-surface`, `--theme-surface-raised`, `--theme-text`, `--theme-text-muted`, `--theme-line`, and `--theme-accent`. Light maps to the canonical Paper/Ink direction. Dark exact values are canvas `#1C1D1D`, surface `#242525`, raised surface `#2D2E2E`, text `#F2F0EA`, muted `#B8B7B0`, line `#85857E`, and accent `#5CB6C3`.
- Superseding the button/select/radio presentations, one native Header `input[type="range"]` is the semantic owner of the compact retro inline SVG switch: `min="0" max="2" step="1"` maps System left (`0`), Light center (`1`), and Dark right (`2`). Its Spanish name and value text expose the stored preference, not the effective theme; direct range selection plus native Arrow Left/Right and Home/End behavior update it. The SVG is `aria-hidden`; visible focus plus desktop/mobile utility placement remain.
- CMD remains fixed at Terminal `#0C0C0C`; Home and established Blog index/article routes adopt semantic colors without structural redesign.

### Evidence

- Genuine locale RED: `tests/locale-system.spec.ts` failed on the candidate because `/en/` lacked the English Home and `Idioma` locale anchors.
- Focused locale, Header, Home, and theme E2E: PASS (15/15), including exact ES/EN copy, bidirectional real anchors/current state, independent dark preference across locale navigation, no future English links, and no overflow at 320/390/768/1024/1440.
- Full E2E: PASS (28/28); production build emits `/` and `/en/` while retaining Blog routes; `git diff --check`: PASS.
- Genuine RED: `tests/theme-system.spec.ts` failed on the committed candidate because no root attributes or theme control existed and Blog retained the dark-only canvas.
- Focused theme E2E: PASS (4/4): system light/dark resolution, persistence/reload/invalid fallback, one native semantic range with exact 0/1/2 mapping, preference-versus-effective distinction, direct selection and native keyboard operation, live system changes versus explicit choices, root/color-scheme state, CMD invariant, responsive no-overflow, and Home/Blog outputs across themes.
- Focused SiteHeader/Home E2E: PASS (4/4 each). Regression E2E: PASS (25/25); production build: PASS; `git diff --check`: PASS.
- RetroLocaleSwitch refinement: locale/Header/theme focused E2E PASS (11/11), including 320/390/768/1024/1440 one-row bounds, no overlap/overflow, native ES → EN tab order, and preserved theme preference across locale navigation.
- Accepted real visual review covered Home ES/EN plus Blog index/article at 320px and 1440px in Light/Dark. Theme and locale controls, contrast, terminal readability, hierarchy, absence of overlap, and URL-driven locale remained intact.
- The visual review discovered Blog index horizontal overflow at 320px: `scrollWidth` 308 exceeded `clientWidth` 305. The Publications title/count desktop flex row did not fit. The bounded fix stacks that row with `flex-column items-start` on mobile and restores `sm:flex-row` on desktop; no copy or route changed.
- Genuine overflow RED: the focused semantic theme test failed on the Blog publications row/mobile overflow. GREEN: focused semantic theme PASS (1/1) and full theme-system PASS (4/4).

## M2.3 — Selected Work Category Carousels

Status: COMPLETE.

### Implemented contract

- Home Hero now targets the one semantic `#featured-evidence` section, headed `Trabajo seleccionado` / `Selected Work` and labelled by `#featured-evidence-title`.
- `src/data/selected-work.ts` supplies locale-equivalent category collections: `RLP / WORK / 001` ImportadorDB and `RLP / WORK / 002` Cosecha en Cope ArtifactSurfaces, plus two independent `RLP / PROD` DossierSurfaces: `001` Águilas FC and `002` La Ola.
- Historical M2.3 snapshot: ImportadorDB was then described with Java 21 and JavaFX 21 while B-003 was unresolved. That version assertion is superseded by the later source-backed detailed case: the current public master manifest declares Java 25, JavaFX 25.0.1, and release 25. Historical migration claims remain omitted.
- Cosecha en Cope is the documented fullstack agricultural marketplace connecting local producers and final consumers. Its compact evidence is restricted to Spring Boot, Spring Security, Hibernate, PostgreSQL, Angular, Thymeleaf, and Swagger; its only action is the established Spanish Blog route `/blog/desarrollo-cosecha-en-cope/`, honestly labelled as Spanish in English. No IoT, JWT, deployment, users, sales, or metric claim is emitted.
- Historical M2.3 snapshot: Production implementation-detail strings were retained in data but not rendered on Home. The later provenance audit withdrew those strings from canonical data because Stitch is visual reference only and no approved factual source supports their publication.
- Visible `Trabajo` / `Producción` and `Work` / `Production` category headings are removed. Each category retains its locale-correct accessible name; the selected-work heading, eyebrow, identifiers, facts, grammar, and CTAs are unchanged.
- `EvidenceCarousel.astro` owns each track and controls. Work and Production have independent, non-looping tracks with native bounded controls and technical mono position footers. Work moves `01 / 02 → 02 / 02 → 01 / 02`; Production remains independently bounded. The first previous and final next buttons are disabled. No autoplay, whole-card link, or nested interactive surface exists.
- Tracks retain overflow, touch/trackpad scrolling, and scroll snap while hiding the native scrollbar. Artifact uses only a semantic accent identity edge; dossier uses semantic accent metadata/current position and a rule, without fills or full frames. Categories stack on mobile and balance into readable columns at desktop. A-proven rendered assets: 0; unclassified local Cosecha media remains omitted.
- Inventory exclusions: Glea Nexo is LAB; Quinta Bella and Fincas Victoria have URLs but no authoritative dossier facts. Neither renders in this slice.

### Final bounded refinement

- Work and Production identifiers now share only the restrained semantic `--theme-accent` treatment; category colors, custom hues, and color-only ranking authority are not introduced.
- Home uses one product-first grammar for every card: technical ID, product name, concise locale-equivalent description, and two or three verified technologies. ImportadorDB remains the Java/JavaFX desktop Excel importer; Cosecha en Cope remains the Java/Angular/PostgreSQL producer-consumer marketplace. Their complete technical facts remain unrendered canonical `facts` in `src/data/selected-work.ts`.
- Production follows that same Home grammar: Águilas FC and La Ola retain only their approved public identity, concise summary, compact technology labels, and truthful destinations. Unproven implementation-detail strings are absent from Home, the Production indexes, and canonical Production data; a future dossier requires its own provenance review. La Ola alone exposes its verified live site, exactly `https://www.laolaart.com/`; Work actions remain only the established Spanish case routes. No manual case/dossier action is invented.
- Carousel footers are normal center/space-between flex rows with stable control targets and no shrink/absolute positioning. They may wrap only when the available mobile width requires it; nothing is hidden or forced to overflow.
- Desktop carousel shells separate a flexible content stage from the normal footer row. The side-by-side Work and Production columns stretch to the same content-valid stage rhythm, keeping their footer rules, counters, and controls aligned through either active item without changing the artifact/dossier asymmetry. At 320–390px the columns stack and retain natural content heights; no desktop stage space is imposed.
- Verification for this bounded refinement: selected-work E2E 8/8; named Home/action/Surface/locale/theme/Header regressions 28/28; full E2E 37/37; production build and `git diff --check` pass. `package.json` defines no lint script.
- This refinement makes no M2.4, header, theme, locale, BaseLayout, Hero, Footer, Blog, taxonomy, membership, or identifier changes beyond the selected-work surfaces.

### Evidence

- Genuine RED: `tests/home-featured-evidence.spec.ts` failed on the accepted fixed work/prod layout because `#featured-evidence`, pure category tracks, Águilas, and independent Production controls did not exist.
- Genuine refinement RED: visible category-heading absence and bounded Spanish/English navigation assertions failed against the prior candidate (visible headings, no bounded labels, and enabled previous control).
- Genuine product-first RED: the featured evidence test failed against the former visible `Context` / `Engineering` / `Evidence` grammar and its resolver/test detail before the data and Home rendering were replaced by summaries plus compact tech highlights.
- Focused selected-work E2E: PASS (6/6), covering exact Work IDs, Cosecha facts/action/category, Glea exclusion, hidden scrollbar with retained overflow/snap, Work and Production bounded navigation/independence/no-autoplay, CTA integrity, ES/EN, themes, and 320/390/768/1024/1440 overflow.
- Focused Home/action/Surface/locale/theme/Header E2E: PASS (26/26). Full E2E: PASS (35/35). Production build and `git diff --check`: PASS; no lint script is defined.
- Geometry stabilization RED: the new text-dependent desktop geometry assertion measured a 25.59375px Work/Production footer mismatch on the candidate before the shared desktop stage shell was applied.
- Featured: PASS (9/9).
- Named regressions: PASS (20/20).
- E2E: PASS (38/38).
- Production build: PASS.
- `npx --yes @google/design.md lint DESIGN.md`: PASS.
- `git diff --check`: PASS.
- Maintainer visual Light/Dark at 320px/1440px: PASS.

## M2.4 — Explore + Footer integration

Status: COMPLETE.

### Implemented contract

- Home now completes the required semantic order: SiteHeader → Hero → Featured Evidence → `#explore` → the single final `SiteFooter`.
- `#explore` is a named bilingual region with locale-equivalent hierarchy and copy. Its primary action opens the real `/blog/` index; lower-priority editorial links open the three established Spanish Blog articles. English labels explicitly identify those article destinations as Spanish.
- Explore uses only real anchors and no placeholders, future routes, removed Home fragments, buttons-as-navigation, or duplicated Footer destinations.
- `SiteFooter` remains the one final footer. Its utility navigation retains Email, GitHub, LinkedIn, and CV, gains a locale-correct accessible name, and uses the public role `Software Developer`. Explore remains editorial; Footer remains compact global utility navigation.
- `SiteHeader` removes the obsolete Spanish Home fragments and now exposes the same truthful destination set in both locales: locale-correct Home and the established Spanish Blog index.
- The integration adds no motion and does not change M2.3 carousel data, mechanics, geometry, Theme, Locale controls, BaseLayout, Blog routes, or future territory scope.

### Evidence

- Focused M2.4 E2E: PASS (2/2), covering semantic order, equivalent ES/EN copy and hierarchy, exact truthful Header/Explore/Footer destinations, keyboard access, Light/Dark, and no overflow at 320/390/768/1024/1440.
- Named Home/action/Surface/locale/theme/Header/M2.3 regressions: PASS (29/29).
- Full E2E: PASS (40/40). Production build: PASS.
- `npx --yes @google/design.md lint DESIGN.md`: PASS with 0 errors (2 existing warnings and 3 informational findings). `git diff --check`: PASS.
- Real browser visual review at 320px and 1440px, ES/EN, Light/Dark: PASS. Home order remains Header → Hero → Featured Evidence → Explore → Footer; desktop Work/Production retain equal dignity and aligned carousel footers; mobile uses natural stacked heights; Explore/Footer remain legible and compact; no horizontal overflow, placeholder destination, cyan overuse, Theme/Locale regression, or M2.3 geometry change was found.
- No historical RED is claimed: the interrupted first writer left implementation in the working tree before a retained pre-implementation failing run could be captured.
- This slice preceded final M2.2 visual acceptance; the accepted review and completed M2 Gate are recorded in their authority sections.

## M2.5 — Responsive/content polish

Status: COMPLETE.

### Implemented refinement

- The bounded responsive/content audit found one candidate-proven paint defect: in Dark, CMD body text could disappear when its Paper color was inherited only from the outer window.
- `.cmd-identity__terminal` now owns `color: var(--color-paper)` directly. This restores stable CMD text painting in Dark without changing copy, geometry, tokens, motion, routes, carousel behavior, or locale structure.
- No unsupported copy or layout change was introduced. The accepted Home hierarchy, product-first evidence, Explore/Footer integration, desktop carousel alignment, and natural mobile heights remain intact.

### Evidence

- Genuine RED: the strengthened theme regression expected the terminal rule to own `var(--color-paper)` and received an empty declaration before the fix.
- Focused Theme: PASS (4/4). Named responsive/content Home/action/Surface/locale/theme/Header/M2.3/M2.4 checks: PASS (31/31).
- Full E2E: PASS (40/40). Production build: PASS.
- `npx --yes @google/design.md lint DESIGN.md`: PASS with 0 errors (2 existing warnings and 3 informational findings). `git diff --check`: PASS.
- Real browser visual review at 320px and 1440px, ES/EN, Light/Dark: PASS. CMD body text is visible in Dark, Home order remains stable, desktop evidence columns retain equal dignity and aligned footers, mobile retains natural stacked heights, and no horizontal overflow was found.
- This slice preceded final M2.2 visual acceptance; the accepted review and completed M2 Gate are recorded in their authority sections.
- **Exclusions preserved:** No routes, new territory implementation, unsupported claims, taxonomy changes, Blog URL changes, motion, or resolution of B-001, B-003, or B-004.

## M2 Gate

Status: COMPLETE. M2 is COMPLETE.

- **Scope settled:** The bounded M2 Home work is accepted; all M2 slices and required visual evidence are complete.
- **Dependencies satisfied:** M2.1, M2.2, M2.3, M2.4, and M2.5 are COMPLETE.
- **Final acceptance evidence:** Focused M2 regressions PASS (31/31); full E2E PASS (40/40); production build PASS (6 pages, 17 optimized images); `npx --yes @google/design.md lint DESIGN.md` PASS with 0 findings; `git diff --check` PASS; accepted maintainer visual review covered Home ES/EN plus Blog index/article at 320px and 1440px in Light/Dark.
- **Delivery state:** No commit was made.
- **Next territory:** Work remains NOT STARTED. No Work, Production, Experience, Education, Notes, About, Contact, or EN parity implementation began.
- **Exclusions preserved:** No routes were added; established Blog URL compatibility remains intact; B-001, B-003, and B-004 remain out of scope.

## Maintainer-authorized post-M2 expansion

The original expansion was authorized after M2 acceptance and did not reopen the completed historical M2 Gate. M2.6 records the bounded corrective work, its technical evidence, and maintainer visual acceptance; it does not invalidate the earlier Gate evidence.

- **Home Explore:** `#explore` now presents two real, equal-priority navigation actions: `/work/` and `/production/`, with `/en/work/` and `/en/production/` as their real English counterparts.
- **Minimal route indexes:** The four routes are limited to bilingual index presentations sourced exclusively from `src/data/selected-work.ts`. They do not constitute completion of the later Work or Production territories, their case studies, or their dossiers.
- **Home About introduction:** A bilingual About section follows Explore in both Home routes. It is a bounded factual introduction, not completion of the later About territory. Profile imagery is structurally optional and must not render until the maintainer provides and approves both an asset and suitable alternative text.
- **Verification:** Focused Playwright PASS (3/3); production build PASS (10 pages, including the four new routes); `git diff --check` PASS; visual review of Home ES/EN and Work/Production at 320px and 1440px in Light/Dark found no horizontal overflow.
- **Scope retained:** Motion and B-001, B-003, and B-004 remain out of scope. The post-M2 territory sequence remains open; this expansion only reduces the future Work, Production, About, and EN-parity scope by the delivered bounded surfaces.

## M2.6 — Explore + About visual fidelity correction

Status: COMPLETE — maintainer visual review accepted.

M2.6 is a corrective slice for the maintainer-authorized post-M2 Home expansion. It does not invalidate or rerun the historical M2 Gate, and it does not imply M3/M4 numbering. The implementation, executable gates, visual matrix, and maintainer visual acceptance are complete.

### Implementation and technical evidence

- **Diagnosis:** Production-preview inspection confirmed that emitted CSS, Astro scoping, cascade, and breakpoints work. The screenshot's layout loss could not be reproduced. Inline Explore SVG was the correctable source-policy defect.
- **Icon correction:** Inline Explore SVG markup was removed. The two decorative external local assets render at `22×22px`, have empty alternative text, and remain outside the accessibility tree.
- **Responsive contract:** Explore meets its equivalent desktop two-column/vertical-divider and mobile stacked/horizontal-divider contract. About retains its title/`[profile.txt]` hierarchy and responsive identity/copy grid. The optional portrait remains non-rendering without an approved asset and alternative text, so no placeholder or CLS is introduced.
- **Executable evidence:** Focused M2.6 + Explore PASS (4/4); Home/Action/Surface/Locale/Theme/Header regressions PASS (20/20); full E2E PASS (43/43); production build PASS (10 pages); `git diff --check` PASS; `npx --yes @google/design.md lint DESIGN.md` exited 0. The PowerShell profile warning is unrelated to the lint result.
- **Visual QA:** PASS for ES/EN × Light/Dark × 320/390/768/1024/1440. No horizontal overflow was found.
- **Maintainer visual acceptance:** The maintainer accepted the final visual review: “Confirmo que después de la revisión visual ha quedado muy bien.” M2.6 is COMPLETE.

### Problem statement and evidence boundary

- The supplied capture shows Explore icons rendered at an unacceptable scale, Explore not forming the intended equivalent two-column desktop composition, and About losing its intended heading hierarchy and desktop grid.
- These symptoms suggest that more than icon artwork may be involved. M2.6 must first inspect emitted CSS, Astro scoping attributes, loaded stylesheets, cascade/specificity, active breakpoints, and browser computed styles before assigning a root cause or changing presentation code.
- No root cause is accepted from the screenshot alone. The implementation record must identify the demonstrated systemic cause with browser evidence and distinguish it from secondary icon-artwork defects.
- The maintainer explicitly prohibits inline SVG for the Explore icons. The final implementation must use external local icon assets, not inline SVG markup, emoji, or font icons. It must not add an icon dependency unless repository evidence demonstrates that local assets cannot satisfy the contract.

### Required visual contract

#### Explore

- Desktop uses two equivalent columns with one restrained vertical divider, compact centered copy, comfortable interactive targets, and no large decorative voids.
- Mobile stacks naturally with one horizontal divider and no horizontal overflow.
- Each icon uses a sober external local asset with a closed visual size of approximately 20–24px. CSS must constrain both its rendered box and intrinsic contribution so missing or unapplied presentation cannot produce oversized artwork.
- Work and Production retain equal priority, real locale-correct destinations, visible keyboard focus, accessible link names, and decorative icons excluded from the accessibility tree.

#### About

- The section title and `[profile.txt]` restore the approved hierarchy and relationship.
- Desktop separates identity metadata and editorial copy into two legible columns; mobile stacks them in a natural reading order without cramped or overlong lines.
- Optional portrait support remains non-rendering when no approved image and alternative text exist. If later supplied, the reserved implementation must preserve intrinsic dimensions/aspect ratio and avoid placeholders and cumulative layout shift.
- ES and EN retain equivalent meaning, hierarchy, spacing, and responsive behavior in Light and Dark.

### Delivery plan — strict RED → GREEN → REFACTOR

#### RED — prove the regression and isolate the system cause

1. Reproduce the reported state from a clean production-equivalent preview and retain browser evidence for ES/EN, Light/Dark, and representative mobile/desktop widths.
2. Inspect emitted CSS and computed values for the Explore navigation, icon boxes, About heading, and About content grid. Record stylesheet presence, scoped selectors/attributes, cascade winner, display/grid values, breakpoint activation, icon dimensions, and overflow measurements.
3. Add focused failing tests that prove the intended two-column/stacked geometry, divider orientation, closed icon dimensions, About hierarchy/grid, optional portrait non-rendering, and absence of inline SVG, emoji, and font-icon dependencies.
4. Confirm that RED fails for the demonstrated defect rather than for unrelated copy, route, theme, or carousel behavior.

#### GREEN — implement only the evidenced correction

1. Correct the systemic style-delivery, scoping, cascade, or responsive issue established during RED; do not patch each symptom independently without explaining the shared failure.
2. Replace Explore's inline SVG markup with two external local icon assets and constrain their rendered geometry to the accepted 20–24px range without adding a dependency unless demonstrated necessary.
3. Restore Explore's equivalent desktop columns and mobile stack, then restore About's hierarchy and responsive identity/copy grid.
4. Preserve bilingual routes, theme tokens, keyboard/focus behavior, semantic section order, and the optional portrait contract.
5. Reach GREEN on focused M2.6 checks before running broader regressions.

#### REFACTOR — simplify without changing accepted behavior

1. Consolidate repeated Explore/About presentation rules only where it reduces fragility and keeps ownership clear.
2. Remove obsolete inline-icon selectors and any workaround made unnecessary by the systemic correction.
3. Re-run focused checks after each cleanup and stop if geometry, accessibility, locale, or theme parity changes.
4. Record final implementation rationale and exact visual evidence in this document and `STATUS.md`; all technical gates and matrix cells are green, and maintainer visual acceptance is recorded.

### Visual acceptance matrix

Real browser review is mandatory for every combination below on both `/` and `/en/`:

| Viewport | Light | Dark |
| --- | --- | --- |
| 320px | Required | Required |
| 390px | Required | Required |
| 768px | Required | Required |
| 1024px | Required | Required |
| 1440px | Required | Required |

Each cell must verify Explore column/stack behavior, correct divider orientation, 20–24px icon rendering, comfortable targets, centered copy, no large voids, About hierarchy/grid, readable line length, optional portrait non-rendering, Theme/Locale integrity, and no horizontal overflow or layout shift.

### Completion gates

- Focused M2.6 Explore + About tests.
- Named Home, Action, Surface, Locale, Theme, and Header regressions.
- Full E2E suite.
- Production build.
- `npx --yes @google/design.md lint DESIGN.md`.
- `git diff --check`.
- Real visual review of the complete ES/EN × 320/390/768/1024/1440 × Light/Dark matrix, with maintainer acceptance.

### Out of scope

- Motion, including hovers, carousel motion, technical background, hero micro-motion, and scroll reveals.
- New territory content or completion of Work, Production, or About.
- Changes to the post-M2 territory sequence or arbitrary M3/M4 milestones.
- B-001, B-003, or B-004 resolution.

## Work — implementation plan

Status: OPEN — the bounded Work index hardening slice is complete; the broader Work territory remains unstarted. This does not reopen M2 or M2.6.

### Completed bounded Work index hardening slice

- The public `/work/` and `/en/work/` indexes retain their two bilingual ArtifactSurface records and established Spanish Blog actions, but no longer render the unproven `facts` field.
- ImportadorDB exposes no Java version on the compact index. B-003 is now resolved for the detailed case by the current public source manifest (Java 25, JavaFX 25.0.1, release 25). Cosecha en Cope exposes no unclassified media, JWT, AWS S3, metrics, deployment, users, or sales claims.
- The Header now exposes the real Work route in both locales and gives it the one truthful primary-navigation `aria-current="page"`; Home is not marked current on either Work route.
- Genuine RED: the focused Work test failed against the former index because two `.selected-index__facts` elements rendered publicly. GREEN: focused Work/Header checks PASS (7/7); named Home/Action/Surface/Locale/Theme/Header/Blog regressions PASS (35/35); full E2E PASS (44/44); production build PASS (10 pages); DESIGN lint PASS; and `git diff --check` PASS.
- Visual QA PASS: `/work/` and `/en/work/` at 320/390/768/1024/1440 in Light/Dark, with no horizontal overflow. This confirms the bounded index only; it is not a claim of completed Work territory visual fidelity against Stitch.

### Starting point and design reference

- `/work/` and `/en/work/` are already delivered as minimal bilingual indexes. The Work implementation extends those routes; it does not duplicate, replace, or downgrade them.
- The available Stitch reference is `Rafael López | Selected Work (Desktop)` (project `2994536844412482857`, screen `8aebc9130a9b485e95414b9f89001f22`, 2560×5740). `Rafael López | Portfolio Home with Selected Work` (screen `918cde6384b44458bd5765daee9b253e`) is complementary, but its detail was unavailable during retrieval.
- Stitch is visual reference only, never factual authority. Before visual fidelity can be claimed, recover and inspect the relevant screens in their rendered state. Translate their intent through the established tokens and components; do not copy generated HTML, invent their unspecified colors, blocks, copy, interactions, or case claims.

### Evidence and case contract

- `src/data/selected-work.ts` remains the factual source for the existing Work index. Its `facts` field is not automatically public case copy: render any fact only when its provenance and contextual meaning are verified for the intended surface.
- The implementation must create an explicit evidence inventory for **ImportadorDB** and **Cosecha en Cope** before adding or expanding case content. Each case surface may contain only verified: real context; architecture or technologies; decisions and evidence; and truthful links.
- A missing source, corroboration, or locale-equivalent statement is a blocking omission: omit the claim or stop for evidence; never fill the gap with inferred outcomes, metrics, deployment claims, screenshots, commits, users, sales, or achievements.
- The established Spanish Blog case URLs remain valid. English may link to them only with an honest Spanish-destination label until a genuine English case exists.

### Delivery plan — strict RED → GREEN → REFACTOR

#### 1. Preflight and reconciliation

1. Reconcile the recovered Stitch screens with `DESIGN.md`, current `SelectedWorkIndex`, `selected-work.ts`, Blog cases, and existing route tests. Record which visual observations are confirmed, unavailable, or in conflict with the design doctrine.
2. Confirm the Work information architecture, case reading order, evidence sources, and route strategy before implementation. Retain the delivered index contract and decide only from verified material whether the next surface is an expanded index, case detail route, or both.
3. Map every planned public statement to a source. Resolve or exclude unproven `facts` before any UI promotion.

#### 2. RED — write failing contractual checks

1. Add focused Work tests for both locales before production changes: route and semantic hierarchy; the two named cases; verified evidence boundaries; truthful Blog links; no unproven facts; and equality of Spanish/English hierarchy and action priority.
2. Add responsive/accessibility assertions for 320/390/768/1024/1440, Light/Dark, keyboard focus, logical DOM order, no horizontal overflow, and no layout shift.
3. If icons are necessary, first prove their labelled semantic purpose. New icons must be external local assets, never inline SVG, emoji, or font icons.

#### 3. GREEN — build only the verified Work territory

1. Reuse the existing layout tokens, `SiteHeader`, `SiteFooter`, `Surface` grammar, and action system. Work uses ArtifactSurface semantics; it must not turn into a generic card grid, a production dossier, or a Stitch HTML copy.
2. Implement ImportadorDB and Cosecha en Cope using the explicit case contract: context, verified architecture/technologies, decisions/evidence, and real links only. Preserve the current concise index records while adding detail only where the evidence inventory permits it.
3. Implement ES/EN in parallel with equal meaning, hierarchy, routes, semantics, and action priority. Keep legacy Blog URLs operational and do not introduce weak English placeholders.
4. Preserve responsive editorial reading order and geometry. Decorations must yield before content; neither viewport nor theme may introduce overflow, hidden meaning, or cumulative layout shift.

#### 4. REFACTOR, QA, and acceptance

1. Consolidate repeated case presentation only when it preserves the factual boundary and makes provenance easier to audit.
2. Run focused Work tests; Home/Action/Surface/Locale/Theme/Header and Blog-compatibility regressions; full E2E; production build; `npx --yes @google/design.md lint DESIGN.md`; and `git diff --check`.
3. Compare the implemented visual intent against the recovered Stitch design at 320/390/768/1024/1440 in Light/Dark for ES and EN. Verify hierarchy, evidence scanability, restrained cyan, reading measure, focus, no overflow, and no layout shift.
4. Record exact evidence in this document and `STATUS.md`. Work is COMPLETE only after maintainer visual acceptance.

### Out of scope

- Production, Experience, Education, Notes, About, Contact, and completion of EN parity.
- Motion, new dependencies unless repository evidence demonstrates necessity, and any arbitrary milestone renumbering.
- B-001 and B-004. B-003 was outside this plan when written and was resolved later by the source-backed ImportadorDB detailed case.

## Work — ImportadorDB detailed case

Status: IMPLEMENTED — evidence sourced from the public ImportadorDB master manifest and code. B-003 is resolved for current technical state: Java 25 and JavaFX 25.0.1. The case omits historical migration claims and unmeasured outcomes.
